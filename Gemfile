# source tells bundler where to download the gems from
source "https://rubygems.org"

gem "gosu"

# group tells Bundler these gems belong to the development and test groups
group :development, :test do
  gem "minitest"
  gem "debug"
  gem "rake"
end